{% include get_template('../table', _self) with {
    cell_class: 'py-2 font-14'
} %}
{% if actions is defined and actions is not empty %}
    <div class="mt-3 d-flex">
        {% for action in actions %}
            {{ action|raw }}
        {% endfor %}
    </div>
{% endif %}

