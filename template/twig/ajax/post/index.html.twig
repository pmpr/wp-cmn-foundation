<div class="pr-select2-list">
    <div class="d-flex justify-content-between">
        <div>{{ title|raw }}</div>
        <div>{{ date|raw }}</div>
    </div>
    <div class="d-flex justify-content-between">
        <div>{{ author|raw }}</div>
        <div>{{ post_type|raw }}</div>
    </div>
</div>

