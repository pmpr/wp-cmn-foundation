<div class="pr-spinner spinner-{{ type|esc_attr }} spinner-{{ type|esc_attr }}-{{ size|esc_attr }}{% if class is not empty %} {{ class|esc_attr }}{% endif %}">
    {{ render_if_has_content('span', {class: 'visually-hidden'}, text) }}
</div>

