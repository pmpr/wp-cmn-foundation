{% extends get_template('./common', _self) %}

{% block field_body %}
    <textarea {{ attributes|parse_attrs|raw }}>{{ value|raw }}</textarea>
    {{ parent() }}
{% endblock %}

{% block after_field %}
    {{ placeholders|raw }}
{% endblock %}

