<{{ args.element|default('button') }} {{ attributes|parse_attrs|raw }}>
    {{ title|raw }}
    {{ render_icon(icon) }}
    {% if args.spinner is defined
        and args.spinner is not empty %}
        {{ render_backend_spinner(args.spinner) }}
    {% endif %}
</{{ args.element|default('button') }}>

