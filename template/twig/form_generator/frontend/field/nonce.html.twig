{% extends get_template('./common', _self) %}

{% block field_body %}

    {% if args.nonce is defined
        and args.nonce is not emtpy %}
        {{ args.nonce|raw }}
    {% endif %}

{% endblock %}
